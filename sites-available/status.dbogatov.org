server {
    server_name status.dbogatov.org;

    include /etc/nginx/snippets/http_redirect.conf;
}

server {
    server_name status.dbogatov.org;

    ssl_certificate /etc/letsencrypt/live/status.dbogatov.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/status.dbogatov.org/privkey.pem;

    include /etc/nginx/snippets/ssl_config.conf;

    location / {
        root /var/php/phpservermon/;
        index index.php index.html index.htm;

        location ~ ^/(.+\.php)$ {

            try_files $uri =404;
            root /var/php/phpservermon/;
            fastcgi_pass unix:/var/run/php5-fpm.sock;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $request_filename;
            include /etc/nginx/fastcgi_params;
        }

        location ~* ^/(.+\.(jpg|jpeg|gif|css|png|js|ico|html|xml|txt))$ {
            root /var/php/phpservermon/;
        }
    }

    include /etc/nginx/snippets/letsencrypt.conf;
}
